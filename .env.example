# Add required environment variable names here. Do not include secret values.
# Default LLM provider (OpenAI API key) reads this from the process environment;
# MicAI never persists it.
OPENAI_API_KEY=
